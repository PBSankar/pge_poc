apiVersion: v1
kind: Secret
metadata:
  name: crm-db-secret
  namespace: crm-app
  labels:
    app: crm-app
    app.kubernetes.io/name: crm-application
    app.kubernetes.io/component: secret
    app.kubernetes.io/part-of: crm-system
type: Opaque
data:
  # Base64 encoded values - Update these with actual credentials
  # Default values: username=root, password=password
  username: cm9vdA==  # base64 encoded 'root'
  password: cGFzc3dvcmQ=  # base64 encoded 'password'
---
# To create your own secret with actual credentials, run:
# kubectl create secret generic crm-db-secret \
#   --from-literal=username=your_username \
#   --from-literal=password=your_password \
#   --namespace=crm-app